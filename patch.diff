--- src/PureScript/Backend/Optimizer/Monomorphize.purs
+++ src/PureScript/Backend/Optimizer/Monomorphize.purs
@@ -582,7 +582,10 @@
               Array.mapMaybe
                 ( \(Tuple specKey info) ->
                     if hasTypeVariables info.instType then Nothing
                     else
                       let
                         definerMod = case String.split (Pattern ".") qualName of
                           parts -> String.joinWith "." (fromMaybe [] (Array.init parts))
+                        _ = unsafePerformEffect (if qualName == "Control.Semigroupoid.compose" && specKey == "2467103119" then Console.log ("Generating compose__2467103119: definerMod=" <> definerMod <> ", modNameStr=" <> modNameStr) else pure unit)
                       in
                         if modNameStr == definerMod then
                           let
