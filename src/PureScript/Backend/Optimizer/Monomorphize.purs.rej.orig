@@ -547,7 +547,7 @@
              filteredArgs = Array.filter (\d -> not (isStatic d)) dictArgs <> normalArgs
           in
              if not (hasTypeVariables genericType) || hasTypeVariables instType then
-               rebuildSpine (Ann ann) f_var' spine'
+               let _rebuild = rebuildSpine (Ann ann) f_var' spine' in if name == "map" then Debug.trace ("REBUILD map! spine=" <> show (Array.length spine) <> " spine'=" <> show (Array.length spine') <> " dictArgs=" <> show (Array.length dictArgs) <> " isPoly=" <> show (hasTypeVariables instType)) (\_ -> _rebuild) else _rebuild
              else
                let specializedName = Ident (name <> "__" <> hashString (mangleType (defaultToAny instType)))
                in case Map.lookup qualName instMap of
